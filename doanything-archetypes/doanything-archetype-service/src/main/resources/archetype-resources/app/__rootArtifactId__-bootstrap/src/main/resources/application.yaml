spring:
  application:
    name: ${rootArtifactId}
server:
  port: 8080
dubbo:
  application:
    name: ${rootArtifactId}-dubbo
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://127.0.0.1:8848
